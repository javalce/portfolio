---
import { cn } from 'tailwind-variants';

interface Props {
  href: string;
  label: string;
  active?: boolean;
}

const { href, label, active } = Astro.props;
---

<a
  href={href}
  aria-current={active ? 'page' : undefined}
  class={cn('text-xs text-foreground/60 transition-colors hover:text-foreground md:text-base', {
    'text-foreground': active,
  })}
>
  {label}
</a>
